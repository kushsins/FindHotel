import { TextField } from "@mui/material";

import { styled } from "@mui/system";
export const StyledTextFelid = styled(TextField)({
  "& .MuiOutlinedInput-root": {
    "& fieldset": {
      borderColor: "#b8d9ff",
    },
  },
});
